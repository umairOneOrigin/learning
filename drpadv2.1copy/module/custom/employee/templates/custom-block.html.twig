{# {{items}} #}

{# <form method='post' action=''>

    {{items.form_build_id}}
    {{items.form_id}}
    {{items.form_token}}

    <div class='form'> {{items.fname}}</div>
    <div class='form'> {{items.sname}}</div>
    <div class='form'> {{items.gender}}</div>
    <div class='form'> {{items.dob}}</div>
    <div class='form'> {{items.save}}</div>

</form> #}

{# {% include "/themes/bootstrap_sbv/layouts/sbv-personenseite-layout.html.twig" %} #}
{# <script> {% include "/themes/bootstrap_sbv/layouts/sbv-personenseite-layout.html.twig" %} </script> #}
{# <style>
    .card{
        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
        transition: 0.3s;
        width: 40%;
        display
    }
    .card:hover {
        box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
    }
    
</style> #}
{# <div class="row" style="padding:">
    {% for record in records %}
        <div class="card" style="width: 20rem;text-align:center">
            
            <div class="card-body"> 
                <h5 class="card-title">Name :{{ record.fname }} {{ record.sname }}</h5>
                <h5 class="card-title">Gender: {{ record.gender }}</h5>
                <h5 class="card-title">DOB: {{ record.dob }}</h5>
            </div>
            
        </div>
    {% endfor %}
<div> #}

{# <ul>
    {% for record in records %}
        <li> {{ record.fname }} </li>
        <li> {{ record.sname }} </li>
        <li> {{ record.gender }} </li>
        <li> {{ record.dob }} </li>
    {% endfor %}
    
</ul> #}
{# <section class="bg-light pt-5 pb-5 shadow-sm">
  <div class="container">
    <div class="row">
      {% for record in records %}
      <!--ADD CLASSES HERE d-flex align-items-stretch-->
      <div class="col-3 mb-3 d-flex ">
        <div class="card">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title" id="nan">Name:{{ record.fname }} {{ record.sname }}</h5>
            <h5 class="card-title">Gender: {{ record.gender }}</h5>
            <h5 class="card-title">DOB: {{ record.dob }}</h5>
          </div>
        </div>
      </div> #}
      {# <!--ADD CLASSES HERE d-flex align-items-stretch-->
      <div class="col-lg-4 mb-3 d-flex align-items-stretch">
        <div class="card">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">Porto Timoni Double Beach</h5>
            <p class="card-text mb-4">Near Afionas village, on the west coast of Corfu island. The two beaches form two unique bays. The turquoise color of the sea contrasts to the high green hills surrounding it.</p>
            <a href="#" class="btn btn-primary mt-auto align-self-start">Book now</a>
          </div>
        </div>
      </div>
      <!--ADD CLASSES HERE d-flex align-items-stretch-->
      <div class="col-lg-4 mb-3 d-flex align-items-stretch">
        <div class="card">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">Tritons Fountain</h5>
            <p class="card-text mb-4">Located just outside the City Gate of Valletta, Malta. It consists of three bronze Tritons holding up a large basin, balanced on a concentric base built out of concrete and clad in travertine slabs.</p>
            <a href="#" class="btn btn-primary mt-auto align-self-start">Book now</a>
          </div>
        </div>
      </div>
    </div>
  </div> #}
  {# {% endfor %}
</section> #}

<div class="row">
    {% for record in records %}
  <div class="col-sm-4 mb-3">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Name:{{ record.fname }} {{ record.sname }}</h5>
        <h5 class="card-title">Gender: {{ record.gender }}</h5>
        <h5 class="card-title">DOB: {{ record.dob }}</h5>
      </div>
    </div>
  </div>
  {% endfor %}
</div>
